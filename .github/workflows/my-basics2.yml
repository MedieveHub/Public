#

name: CI-pushtomaster

on: 
 push:
  branches:
    - master
jobs:
  test-push:
  
    runs-on: pushaction
    
    steps:
    - name: Print starting message
      run: echo "hotfix commited"
    
  deploy:
  
    runs-on: pushaction
  
    steps:
    
    - name: Print starting message 2
      run: echo "hotfix commited"
  
